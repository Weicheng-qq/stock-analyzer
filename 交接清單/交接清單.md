# 美股 AI 深度分析網站 — 完整交接清單

> **用途**：開新 session 或換新電腦時，只要讀完這份文件，就能完全記得這個網站的所有細節、架構、鐵則與踩過的坑，不需要重新解釋。
> **最後更新**：2026-08-01（Q2 2026 財報季更新進行中，見第 9 節）
> **公開網址**：https://weicheng-stock.vercel.app
> **原始碼位置**：`C:\Users\Amber Lin\weicheng claude\股票\`

---

## 0. 一句話定位

> 一個以「**以事實為依據、絕不 AI 臆測**」為最高鐵則的美股個股深度分析單頁網站，投資哲學是「**以合理低估價格，買入擁有強大護城河的公司**」。所有財務數字來自官方來源（SEC / Yahoo / 公司官網 IR），每間公司的「產品」與「法說會前景」一律**優先從公司官方網站的投資人關係（IR）專區**手動判讀寫入，找不到才退回 SEC 官方文件。

---

## 1. 網站架構（技術）

### 1.1 檔案結構
- **`stock_analyzer.html`** — 本機開發版（單一 HTML 檔，內含全部 HTML/CSS/JS，無框架、無 build）。
- **`index.html`** — 線上部署版。
- **鐵則：兩檔內容必須完全一致。** 每次改完 `stock_analyzer.html` 一定要執行：
  ```bash
  cp stock_analyzer.html index.html
  ```
  再一起 commit。（可用 `diff -q stock_analyzer.html index.html` 驗證。）
- **`CLAUDE.md`**（專案根目錄）— 本專案專屬提醒，Claude Code 自動載入。
- **`專案交接文件.md`**（專案根目錄）— 更早期的技術踩坑與函式位置紀錄。
- **`交接清單/`**（本資料夾）— 這份文件 + 股票APP Skill + Excel 精神紀錄。

### 1.2 部署流程
- 部署平台：**Vercel（免費方案）**，綁定 GitHub repo。
- 流程：`git push origin main` → Vercel 自動偵測並部署，**約 1 分鐘**上線。
- 沒有伺服器、沒有資料庫，是純靜態單頁應用（SPA）。

### 1.3 資料來源與代理
- **財務數字**：SEC EDGAR（`data.sec.gov/submissions/CIK{10碼}.json`）＋ Yahoo Finance。
- **AI 分析**：使用**免費**的 AI 模型（遵守零費用鐵則）。
- **CORS 代理**：`/api/proxy`（Vercel serverless function）用來抓 SEC 全文、繞過瀏覽器 CORS。
- **10 年財務**：`isAnnualForm` 接受 10-K / 20-F / 40-F / 6-K；`extractAnnual` 用「財年結束月份」比對；`pickUnit(units, instant)` 挑年度資料最多的幣別。

### 1.4 前端渲染關鍵函式
- `renderAI()` — 把 AI 分析結果與 IR 常數渲染到頁面。
- `#ai_guidance` — 渲染「法說會 CEO 前景展望」，資料來自 `IR_CALL_SUMMARY`。
- `#ai_products` — 渲染「產品與服務」，資料來自 `IR_PRODUCTS_SUMMARY`。
- **注意：IR 內容在 AI 分析完成後才會渲染出來**（不是一載入就顯示）。

### 1.5 Service Worker（重要踩坑）
- 曾因 Service Worker 造成**重新整理無限迴圈**（我自己寫的 kill-switch `c.navigate(c.url)` 造成迴圈）。
- **現狀**：SW 已被改成「純 kill-switch」——只做 `unregister` + 清 caches，**不做 navigate、不 claim**；HTML 已**不再註冊** SW。不要再把 SW 加回來。

---

## 2. 三大核心鐵則（最重要，違反等於做錯）

### 鐵則一：以事實為依據，絕不 AI 臆測
- 數字**只能**來自官方來源（SEC EDGAR / Yahoo / 公司官網 IR）。
- **絕不可**用 AI 編造、用常識或訓練記憶臆測，**不做預測性斷言**。
- 定性欄位（產品／競爭對手／護城河等）**禁止**回「無公開資料」——要用官方年報＋維基組出實際內容；**只有真正查無的「數字」才可留白**。
- 遇到「AI 好像漏抓某事實」→ 先查**官方文件全文**（用 `/api/proxy` 抓 SEC 全文），**不要**放寬 prompt 讓 AI 用常識硬補。

### 鐵則二：法說會與產品，一律「公司官網 IR 優先」
- 每間公司的「法說會 CEO 前景展望」都要**先從該公司官網「投資人關係(IR)」**找：法人說明會逐字稿、簡報、財務報表、營運績效報告、線上會議影片，擷取重點（**特別是最新成長展望＋資本支出**）。
- **官網找不到，才退回 SEC 官方新聞稿。**
- **產品**也一樣：從公司官網抓最新一季的產品／營收結構（比年報新）。
- 使用者原話：**「所有搜尋的公司，產品和法說會請從公司網址去抓最新的資訊」**、**「與台積電邏輯一樣」**。

### 鐵則三：修正後一定自我驗證
- 完成任何修正／實作後，**必須自己先驗證**（跑測試、實際操作網站、檢查輸出、語法檢查）。
- **不可以讓使用者自己測試時才發現問題。** 前端變更要在瀏覽器實際操作 golden path 與邊界情況再回報完成。

---

## 3. 法說會／產品「官網 IR 優先」的實作細節

### 3.1 三個常數（都以股票代碼 keyed，人工判讀寫死）
| 常數 | 內容 | 渲染位置 |
|---|---|---|
| `IR_CALL_SUMMARY` | 法說會前景展望（最新一季） | `#ai_guidance` |
| `IR_PRODUCTS_SUMMARY` | 產品／營收結構（最新一季） | `#ai_products` |
| `IR_QUARTERLY_PAGE` | 公司官網 IR 法說會頁連結 | 來源連結 |

- 每筆物件格式：`{ quarter, source, html: \`...\` }`。
- 這三個常數的內容**優先於**原本的 SEC／AI 邏輯。
- **競爭對手**因法說材料不涵蓋，維持年報來源（不進這三個常數）。

### 3.2 抓取技巧（官網常擋 curl）
1. 官網常有 **Cloudflare / Akamai** 擋 curl。
2. 用**內建瀏覽器**（`mcp__Claude_Browser__`）登入該網域取得 session cookie，`read_page` 拿檔案連結。
3. 再用「**瀏覽器 cookie + 桌面 UA**」curl 下載 PDF。
4. 逐字稿 PDF 用 `pdftotext -enc UTF-8` 解析（poppler 已裝，Python 無 PDF 套件）。
5. 簡報 JPG 可直接用 Read 判讀，且常同步申報在 SEC。
6. **官網 CDN**（q4cdn / cloudfront）多可直接 curl（例：GOOGL=abc.xyz、META、AMZN）。
7. **SEC 備援**：SEC 8-K 的 EX-99.1（外國公司為 6-K）就是公司自己申報的**同一份**官方新聞稿。EDGAR 抓法：`data.sec.gov/submissions/CIK{10碼}.json` → 找 item 2.02 的 8-K（或 6-K）→ 用 `index.json` 列出檔案 → 抓 exhibit（檔名多含 `99` 或 `earningsrelease`）。

### 3.3 金額換算規則
- 美元 billion → **億**（1B = 10 億）。
- 保留原幣別：韓元用「兆韓元」、歐元用「億歐元」、英鎊／美元照標。
- **絕不做 AI 幣別換算**（只照官方原文換算單位）。

### 3.4 已知擋 curl 的公司（退回 SEC 同一份文件）
- **TSLA / LLY（禮來）** 用 Akamai bot manager，TLS 指紋擋 curl，**即使帶 cookie 也 403/000** → 退回 SEC 同一份 8-K EX-99.1。
- 其餘官網擋 curl 者，一律退回 SEC 同一份官方文件。
- **使用者明確要求**：能從官網抓就從官網抓，不要走 SEC 捷徑（雖 EX-99.1 是同一份，但官網常有更完整的簡報／逐字稿）。

### 3.5 新增一間公司的標準流程
1. 內建瀏覽器開該公司官網投資人關係頁、取 cookie ＋桌面 UA。
2. curl 下載最新一季「投資人簡報／逐字稿 PDF」。
3. `pdftotext -enc UTF-8` 解析。
4. 逐項核對後寫進 `IR_CALL_SUMMARY` ＋ `IR_PRODUCTS_SUMMARY` ＋ `IR_QUARTERLY_PAGE`（數字全照官方原文、金額換算億／兆、標幣別）。
5. **驗證**：`grep -c "'SYM': {"` 應等於 **2**（call + products 各一）；用 Node.js `new Function` 編譯三個常數確認無語法錯誤；`cp` 同步後 `diff -q`。

### 3.6 兩個物件 anchor 容易搞混（踩過的坑）
- 新增／更新時：**先寫 `IR_CALL_SUMMARY` 的區塊、再寫 `IR_PRODUCTS_SUMMARY`**。
- 兩個物件的 anchor（同一句話）容易搞混——曾把 call 內容誤植到 products。
- 解法：**用「前一檔的獨特句子」當 anchor** 來區分要插入的位置；寫完 `grep -c` 確認 = 2。

---

## 4. 目前已建置的公司（自選股 IR 官網優先全數完成，50+ 檔）

### 首批 30
TSM、NVO、INTC、LLY、NVDA、MU、GOOGL/GOOG、AAPL、META、MSFT、AMZN、TSLA、AMD、AVGO、ASML、QCOM、UBER、NFLX、BKNG、JPM、JNJ、PFE、COST、DELL、KO、SBUX、DIS、MAR、IHG、SKHY。

### 續批 21（金融／資安／AI 網通半導體／消費／能源／醫療／工業／航太）
GS、BAC、MS、ORCL、PLTR、IBM、V、MA、WMT、PEP、MCD、XOM、CRWD、PANW、ANET、MRVL、SMCI、ARM、ABNB、VRT、CVX、UNH、ABBV、AMGN、LRCX、CAT、GE、T、NKE、SPOT、BA、SHOP、PYPL。

### 特別註記
- **INTC、GOOGL 已更新至 2026 Q2**。
- **SHOP** 已從外國申報人（6-K）改用**美國國內申報（8-K）**。
- **AAPL**：不公布書面財測（展望於法說會口頭），已於摘要註明。
- **ARM**：SEC 的 8-K 附件只是「指向官網股東信」的通知、不含數字 → 改抓官網股東信（SEC 6-K exhibit 99.2）取得完整財報。
- **SPCX（SpaceX）無法做**：未上市、無公開財報／官方 IR 法說會。

---

## 5. 每季主動維護責任（在我，不等使用者提醒）★重點★

- **主動更新責任在 Claude**：每次與使用者對話（**不限主題**）都要主動檢查已建置公司及自選股，有無「比常數裡更新的一季」尚未更新；有的話**主動提出並立刻依 IR 優先原則更新**。
- **使用者三次強調（原話）**：
  - **「INTEL 和 Google 有新法說會怎麼沒更新」**
  - **「你可以自己每季法說會後自己去更新，不要我提醒你才去做」**
  - **「以後若有其他公司的最新法說會，你要自動去更新網站資訊，不要被我發現你沒更新最新資訊」**
- **落實做法**：開場（或任何一次對話）先快速比對「今天日期 vs 各公司財報時程」，凡是已發布但常數還停在舊季的，**主動抓官網 IR 更新、不用等使用者開口**。
- **時程參考**：
  - TSM：約每年 1／4／7／10 月中（站上 2026 Q2、下次 2026 Q3 約 10 月中）。
  - NVO：約 2／5／8／11 月初（站上 2026 Q1、下次 2026 H1 約 2026/8/5）。
  - 美股多在 1／4／7／10 月下旬密集發布財報。
- **全自動排程不採用**：可能計費（違反零費用）、且背景排程拿不到互動瀏覽器、必卡 Cloudflare。折衷＝每次對話主動檢查。

---

## 6. 版面／視覺（已定案，勿主動改版）

- **版面已定案，勿主動提議改版**（使用者已否決過 5 種改版風格）。
- 網站名稱：**美股 AI 深度分析**（含對應 logo）。
- 預設自選股：**TSM / NVO**。
- 網站介紹區塊 `#welcome`：**永久釘在標題與自選股之間、不隱藏**；**2×2 並排版面**；**投資總結獨立為最後一項**。
- 首屏投資哲學文案：**「以合理低估價格，買入擁有強大護城河的公司」**（使用者要求把多餘的「的」刪掉的那個版本）。
- 財務表格顯示 **10 年**（曾只顯示 7 年，已修）。
- 手機版自選股名稱不可截斷；名稱要正確（DELL＝戴爾非台達電；V＝威士卡非「視覺」）。
- 已移除「原文翻譯」連結。

---

## 7. 全域偏好（適用所有專案，來自 `~/.claude/CLAUDE.md`）

1. **一律免費，禁止任何付款**：所有 Claude 操作（API/MCP/CLI）必須免費；禁止刷卡／Google 付費／LINE Pay；任何可能產生費用的方案前，**先詢問並取得明確同意**；預設一律免費／開源。
2. **一律繁體中文回覆**：專有名詞可中譯並括號附英文縮寫（例：應用程式介面（API））。
3. **修正後自我驗證**（同鐵則三）。
4. **「用 Claude 賺錢」類需求** → 主動建議可用的 Skill / API / MCP / CLI（仍守零費用）。
5. **快滿版／AI 變笨時** → 提醒使用者並建立／更新交接手冊（PreCompact hook，`matcher: auto`）。

---

## 8. 環境與踩坑速查（Windows / Bash / Python）

- 主要工作目錄：`C:\Users\Amber Lin\weicheng claude\股票`。
- Shell：PowerShell（主）＋ Bash（POSIX）。Python 讀檔用 **Windows 路徑（`C:/...`）非 `/c/...`**。
- Python 輸出中文：用 `PYTHONIOENCODING=utf-8` ＋ `sys.stdout.buffer.write(...encode())`，避免 cp950 UnicodeEncodeError。
- 避免在 inline python-in-bash 用會被 shell 吃掉的字元；**多行內容改寫成 `.py` 檔**再執行（曾出現 `[d.]+: command not found`）。
- SEC 抓取一定帶合規 UA（含 email），例：
  `Mozilla/5.0 (...) AppleWebKit/537.36 stock-research aa910517@gmail.com`。
- 抓 exhibit 用該筆 filing 的 `index.json` 列檔最穩（檔名不一定含 `99`，也可能是 `...earningsrelease.htm`）。
- 語法驗證：用 Node.js `new Function(objSrc)` 編譯三個常數；template literal 內小心 `` ` `` 與 `${`（我寫的內容都避開）。

---

## 9. 尚待處理／注意事項

### 🔴 2026-08-01 進行中：Q2 2026 財報季更新（美股 7 月底密集發布）
用 SEC EDGAR 官方申報日（8-K item 2.02）確認「常數停在舊季但已有新一季財報」的落後清單，依 IR 官網優先原則更新。工具腳本在 scratchpad：`check_earnings.py`（查各檔最新財報8-K日）、`fetch_er.py TICKER`（抓最新EX-99.1新聞稿全文）；需先下載 `cik_map.json`（SEC company_tickers.json）。Apple 這種分類數字不在新聞稿的，改抓當日/數日內的 10-Q。
- **✅ 本 session 已更新（7 檔，含 CALL＋PRODUCTS 兩常數，來源官網newsroom＋SEC，已 commit＋推送）**：
  - AAPL → FY2026 Q3（營收1094億+16%、iPhone543/Mac104/Services307億創六月季新高、依10-Q分類與地區）
  - MSFT → FY2026 Q4（營收900億+18%、Azure全年破千億、雲端RPO6780億+84%、Q4 capex358億/全年1159億）
  - META → Q2 2026（營收608億+28%、DAP36.0億、Q3財測610-640億、全年capex1300-1450億）
  - AMZN → Q2 2026（營收2006億+20%、AWS422億+37%為18季最快、Q3財測1970-2020億）
  - V → FY2026 Q3（淨營收116億+14%、processed txn+10%、回饋62億）
  - MA → Q2 2026（淨營收93億+14%、GDV+8%、營益率60.2%、Agentic Payment）
  - QCOM → FY2026 Q3（營收99億-4%、車用15.9億+61%連23季雙位數、Q4財測97-105億、非手機2029達400億願景）
- **✅ 第二批也已完成（9 檔，本 session 已全數更新＋commit＋推送）**：KO(Q2,上調全年財測)、XOM(Q2,上游產量20年新高)、CVX(Q2,產量+20%、微軟20年供電)、ABBV(Q2,免疫87.86億)、VRT(Q2,+24%上調全年AI資料中心)、BA(Q2,積壓訂單創高7150億FCF轉正)、PYPL(Q2,上調全年財測)、SBUX(FY26Q3,同店+7.9%連4季)、TSLA(Q2,交車48萬創新高、官網Akamai擋curl→用SEC EX-99.1)。
- **✅ 本輪 16 檔美股 Q2 2026 財報全數更新完成**（4大巨頭+V/MA/QCOM+上述9檔）。
- **✅ NVO 已查**：2026-07-31 的 6-K **不是財報**，是 ZEUS 三期心血管試驗未達主要終點(ziltivekimab MACE 未優於安慰劑)的臨床公告；NVO 財報常數維持 Q1，H1/Q2 財報仍約 8/6 才發，屆時再更新。
- **尚未發 Q2（SEC 查無新申報、下週才發，勿動）**：AMD、PLTR、UBER、ABNB、MCD、PFE、AMGN、CAT、MAR、BKNG、ANET、SHOP、LLY、DIS 等。

### 本 session 其他已完成（UI/bug，均已 commit＋推送）
- **自選股拖曳排序**：▲▼ 按鈕改為「✏️編輯模式＋☰拖曳」（pointer事件、touch-action:none 支援手機）；已於 390px 實測。
- **修正開 App 股價顯示舊值**：手機從背景喚回(PWA/bfcache)時 JS 不重跑、價格停舊值。新增 `refreshLiveData()`，監聽 `visibilitychange`/`pageshow persisted`(強制)/`focus`，回前景就地重抓自選股股價+走勢+指數（15秒節流）。
- **自選股字體放大**：logo 30→34px、公司名/股價 14.5→16px、代碼 11→12px、漲跌幅pill 12.5→14px（手機組），基礎組同步。
- **設定金鑰 Modal 改上架友善版**：為上架 Google Play 準備。伺服器 `/api/ai` 已用伺服器端金鑰免費代跑 AI（`useServer=!gmKey&&!isLocal`），使用者免設金鑰即可用；原本開設定跳出一大串 API 金鑰教學不適合上架。改成預設綠框「✅AI已內建、免設定即可使用」，金鑰教學+輸入框收進「進階(選填)」`<details>` 摺疊區。已 390px 實測。

### 🟡 台股品質問題（重大，已定方向、進行中）
- **診斷**：台股(`doSearchTW`)與美股(`doSearch`)是兩套邏輯。美股的產品/護城河/競爭對手/CEO展望用「人工查官方IR寫死的事實」(IR_CALL_SUMMARY等)；**台股全部丟給 AI 現編**(只餵名稱+股價+4年Yahoo財務、無官方來源)→ 內容糟、且與美股同公司不一致(使用者反映：台積電台股vs美股內容不同、群聯內容糟透)。**這違反本站第一鐵則「以事實為依據、絕不AI臆測」。**
- **已定方向（使用者選）**：**雙掛牌先共用美股事實內容**。已做首檔：新增 `TW_US_EQUIV={'2330':'TSM'}`，`doSearchTW` 對雙掛牌股把 `window.__symbolForLinks` 指向美股代碼→`renderAI` 就會渲染該美股已建的官方產品/法說會展望事實(與美股一致)，股價仍顯示 NT$；AI生成區塊(護城河/競爭對手)註明「即美股同一實體」以真實業務分析。**已 commit＋推送**。
- **✅ 純台股 AI 臆測問題已根治（本 session，關鍵）**：台股原本 `__wikiData` 等全設 null → products/competitors/moat 全靠 AI 腦補。新增 `fetchWikipediaZh(name,code)` 抓**中文維基百科**(zh.wikipedia，自帶 CORS)，`doSearchTW` 在 AI 呼叫前填入 `window.__wikiData`；台股定性內容改以真實維基事實為準、畫面顯示「✅依維基百科整理」+維基連結。**已於線上 Vercel 端到端實測群聯 8299**：產品=NAND控制晶片/SSD/隨身碟/記憶卡控制晶片、競爭對手=慧榮(Silicon Motion)/美光/三星、排名=全球獨立NAND控制晶片龍頭，全部正確有來源(從「糟透了」變準確)。
- **✅ 台股 AI 提示詞已語境化（本 session）**：`callGemini` 台股 ctx 加 `market:'tw'`，提示詞依市場改「你是專業台股分析師」並註明台灣上市/上櫃、金額以新台幣為主、定性以中文維基為依據；美股行為不變。已 Node 編譯驗證＋線上實測台達電(2308)渲染正常。
- **⏳ 台股仍可再優化(非緊急)**：①中文維基較短的小型股(如群聯僅160字)內容有限，可改良 `condenseWiki`／中文維基抓 產品/歷史 分節補內容;②要更嚴謹到「官方級」仍需 MOPS 公開資訊觀測站/公司IR法說會(MOPS 曾被 proxy 擋403需先解)——但維基版已大幅改善、符合事實鐵則;③雙掛牌美股事實內容目前僅台積電(TSM);聯電/日月光/中華電 ADR 未建。

### 版面/視覺（本 session 追加，皆已上線）
- **自選股字體放大**：logo 30→34、公司名/股價 14.5→16、代碼 11→12、漲跌幅 12.5→14px。
- **指數列字體放大**：加權/櫃買/四大指數 名稱 14.5→16、數值 14→17、漲跌幅 12.5→14.5px（與自選股一致）。

### 📱 Google Play 上架相關（使用者目標）
- App 之後要上架 Google Play（PWA 包 TWA/Bubblewrap）。凡涉及「一般使用者會看到的畫面」都要以此為前提：不要露出技術性設定、金鑰、debug 訊息。設定 Modal 已處理(見上)。

### ⚠️ 手機預覽鐵則（使用者三令五申，務必遵守）
- **驗證與預覽一律用「手機 App 畫面」，永遠不要讓使用者看到桌面網頁畫面。**
- 踩坑：每次 Edit 存檔後，編輯器 PostToolUse hook 會自動把「檔案靜態快照(file://，桌面寬、不跑JS)」推到 Browser 面板，蓋掉手機預覽 → 使用者就會看到討厭的桌面畫面。
- 正解：用 `preview_start`(name:stock-preview，launch.json 已設本機 http.server 指向股票資料夾) 開 localhost 預覽，`resize_window` 設 390×844，每次要給使用者看之前用 `tabs_select` 把手機分頁拉到最前面再截圖。**收尾一定停在手機畫面。**

### 每季維護通則
- **每季**：各檔法說後人工更新對應 `IR_CALL_SUMMARY` ＋ `IR_PRODUCTS_SUMMARY` ＋ `IR_QUARTERLY_PAGE`（主動、不等提醒）。
- 新增公司照第 3.5 節標準流程。
- 保持 `stock_analyzer.html` === `index.html`（改完 `cp stock_analyzer.html index.html` 再 commit）。
- 不主動改版、不加回 Service Worker、不走 SEC 捷徑（能官網就官網）。

---

## 10. 相關檔案索引

- `stock_analyzer.html` / `index.html` — 主程式（必一致）。
- `CLAUDE.md` — 專案專屬提醒（進度追蹤在此）。
- `專案交接文件.md` — 早期技術踩坑。
- `交接清單/交接清單.md` — 本文件。
- `交接清單/Skill.md` — 「股票APP」技能（用同邏輯做台股 APP）。
- `交接清單/skill架構內容.xlsx` — 使用者原話原則紀錄（網站精神）。
- `.claude/skills/股票APP/SKILL.md` — 可用 `/股票APP` 觸發的正式技能（與本資料夾 Skill.md 同步）。
