{
  "name": "stock-analyzer-scripts",
  "private": true,
  "type": "module",
  "description": "每日法說會自動更新腳本的相依套件。刻意放在 scripts/ 而非專案根目錄——根目錄若有 package.json，Vercel 會改用 Node 專案的方式部署，而本站是純靜態 HTML，不該受影響。",
  "dependencies": {
    "playwright-core": "^1.50.0"
  }
}
