# 永久運作手冊

> 這份手冊寫給**沒有 Claude 協助時的你**。App 平常會自己運作，你只需要做一件事：
> **收到 GitHub 寄來的「健康檢查失敗」信時，照這份手冊處理。**
>
> 正式網址：**https://weicheng-stock.pages.dev**　　最後更新：2026-09-19

---

## 一、App 平常自己在做什麼（你不用做任何事）

| 自動工作 | 什麼時候 | 做什麼 |
|---|---|---|
| 網站 | 24 小時 | 由 Cloudflare 免費託管，改程式後 `git push` 就自動更新 |
| 每日法說會更新 | 每天早上 7:00 | 找出前一天有新財報的公司，用 AI 分析後存進網站 |
| **每日健康檢查** | 平日早上 9:30 | 檢查股價、財報、AI、權杖是否正常，**有問題就寄信給你** |
| AI 模型自動換新 | 每 6 小時 | Google 下架舊模型時，自動改用還能用的新模型，不用改程式 |
| 防止排程被停用 | 每次健康檢查 | GitHub 規定 60 天沒動靜就停掉排程，45 天時會自動補一個心跳 |

**全部免費**，而且沒有綁任何信用卡，所以**不可能產生費用**。免費額度用完時只會「暫停」，不會「收費」。

---

## 二、你唯一要做的事：確認收得到通知信

1. 登入 GitHub，打開 👉 https://github.com/settings/notifications
2. 往下找到 **Actions** 區塊
3. 確認 **Email** 有勾選，並選 **Only notify for failed workflows**（只在失敗時通知）

之後只要健康檢查失敗，就會收到一封主旨類似 **「Run failed: 每日健康檢查」** 的信。

---

## 三、收到失敗通知信時

1. 點信裡的連結，會打開 GitHub 的執行紀錄頁
2. 頁面中間有一張**中文表格**，❌ 的那一列就是出問題的項目，表格下面還有「怎麼處理」
3. 對照下表處理

| ❌ 的項目 | 原因 | 你要做什麼 |
|---|---|---|
| **網站首頁** | Cloudflare 部署失敗 | 見〈四-1 重新部署〉 |
| **台股報價（證交所）** | 證交所改版或封鎖 | **你無法自己修**。App 會顯示「—」，**不會顯示錯誤股價**。持續超過一週才需要找人改程式 |
| **美股報價（Yahoo）** | Yahoo 改版 | 同上 |
| **美股財報（SEC）** | SEC 改規則 | 同上，很少發生 |
| **AI 模型設定** | Gemini 金鑰失效 | 見〈四-2 換 Gemini 金鑰〉 |
| **AI 分析產生** | 當天免費額度用完 | **先等一天**，隔天通常自動恢復。連續 3 天以上才照〈四-2〉處理 |
| **AI 快取權杖（GitHub）** | GitHub 權杖到期或被刪 | 見〈四-3 換 GitHub 權杖〉 |
| **每日法說會分析有新資料** | 排程的金鑰失效 | 見〈四-4 換排程的金鑰〉 |
| ⚠️ **舊網址轉址**（只是提醒） | Vercel 轉址失效 | 只影響用舊連結的人，可以不理 |

💡 **偶爾失敗一次不用緊張**，每一項都會重試 3 次才判定失敗。如果隔天的檢查自己恢復綠色，就不用處理。

---

## 四、常用修復步驟

### 四-1　重新部署（網站首頁異常時）

1. 打開 https://dash.cloudflare.com → 左側 **Workers & Pages** → 點 **weicheng-stock**
2. 上方 **Deployments** 分頁，看最上面那一筆是不是紅色 **Failed**
3. 點那一筆最右邊的 **⋯** → **Retry deployment**

### 四-2　換 Gemini 金鑰

1. 打開 https://aistudio.google.com/apikey → **Create API key** → 選 **Default Gemini Project** → 複製
   - ⚠️ 畫面上的 **Set up billing**、**Upgrade** 一律不要點
2. 打開 Cloudflare → **Workers & Pages** → **weicheng-stock** → **Settings** → **Variables and secrets**
3. 找到 `GEMINI_KEY` → 編輯 → 貼上新金鑰 → **Save**（類型要是 **Secret**）
4. 照〈四-1〉重新部署一次，新金鑰才會生效

### 四-3　換 GitHub 權杖（**2027 年 9 月 19 日到期**）

GitHub 會在到期前一週寄提醒信。到期後網站照常運作，只是 AI 分析結果不會再存成共用快取。

1. 打開 https://github.com/settings/personal-access-tokens
2. 點 **cloudflare-pages-ai-cache** → **Regenerate token** → 有效期限選一年後 → 確認
3. 點複製按鈕 📋（⚠️ **不要截圖**，這串字只會出現一次）
4. 打開 Cloudflare → **weicheng-stock** → **Settings** → **Variables and secrets** → 編輯 `GITHUB_TOKEN` → 貼上 → **Save**
5. 照〈四-1〉重新部署

### 四-4　換排程的金鑰（每日法說會停止更新時）

每日排程在 GitHub 上有**自己的一份**金鑰，跟網站的是分開的。

1. 打開 https://github.com/Weicheng-qq/stock-analyzer/settings/secrets/actions
2. 找到 `GEMINI_KEY` → 右邊鉛筆圖示 → 貼上新的 Gemini 金鑰（取得方式同〈四-2〉第 1 步）→ **Update secret**
3. 不用重新部署，隔天早上 7:00 的排程就會用新金鑰

### 四-5　手動跑一次健康檢查（修完後確認）

打開 https://github.com/Weicheng-qq/stock-analyzer/actions/workflows/health-check.yml
→ 右邊 **Run workflow** → **Run workflow**，約 2 分鐘後會出現結果。全部 ✅ 就代表修好了。

---

## 五、絕對不要做的事

| ❌ 不要 | 為什麼 |
|---|---|
| 在 Google Cloud / AI Studio **啟用帳單**（Set up billing） | 一啟用，超過免費額度就會**開始收費**；不啟用的話，超額只會被拒絕 |
| **刪除 Vercel 專案** | `weicheng-stock.vercel.app` 會被別人註冊走，舊連結可能變成釣魚網站 |
| 把金鑰、權杖**貼到聊天、截圖、email** | 出現過就要當作外洩，得重新產生 |
| 刪除 GitHub 權杖 **cloudflare-pages-ai-cache** | 網站的 AI 快取正在用它 |
| 在 `vercel.json` 加回任何設定、恢復 `api/` 資料夾 | 所有程式都已搬到 Cloudflare 的 `functions/api/` |
| 點任何寫著 **Upgrade**、**Pro**、**$** 的按鈕 | 全部都是付費方案 |

---

## 六、免費額度用完時會怎樣

| 服務 | 免費額度 | 用完時 | 何時恢復 |
|---|---|---|---|
| Cloudflare（網站＋股價＋AI） | 每天 10 萬次請求 | 當天股價、AI、財報暫停 | **台灣時間早上 8:00** 自動恢復 |
| Gemini（AI 分析） | 每日額度 | 自動改用備援模型；都用完就暫停 | 隔天自動恢復 |
| GitHub Actions | public repo 無限制 | — | — |

⚠️ **都不會收費**。股價每秒更新很耗額度，所以 App 在你 2 分鐘沒碰螢幕時會自動降為每 5 秒更新（畫面上會標示）。

---

## 七、將來要改程式時

- 程式碼：https://github.com/Weicheng-qq/stock-analyzer
- 給 AI 或工程師看的交接資料：`交接清單/` 資料夾、`CLOUDFLARE-PAGES部署.md`
- 主要檔案：

| 檔案 | 用途 |
|---|---|
| `stock_analyzer.html` ＝ `index.html` | 整個 App（**兩個檔案內容必須完全一樣**） |
| `functions/api/proxy.js` | 股價、財報代理 |
| `functions/api/ai.js` | AI 分析（含模型自動換新） |
| `scripts/lib/ai-call.mjs` | 每日排程用的 AI（跟上面同一套自動換新規則） |
| `scripts/health-check.mjs` | 每日健康檢查的檢查項目 |
| `.github/workflows/` | 兩個自動排程的設定 |

改完程式後，可以先執行 `node scripts/check-html.mjs` 檢查有沒有改壞，再 `git push`。
